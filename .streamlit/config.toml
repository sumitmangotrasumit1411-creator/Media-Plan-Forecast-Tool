[server]
maxUploadSize    = 3072          # 3 GB
enableCORS       = false
enableXsrfProtection = true
headless         = true          # required for Docker / cloud deployments
port             = 8501
# address is NOT set here — Streamlit Cloud manages its own binding.
# For local development, run with: streamlit run app.py --server.address 127.0.0.1

[runner]
fastReruns       = true
magicEnabled     = false         # disable magic commands for performance

[browser]
gatherUsageStats = false         # no telemetry

[theme]
base             = "light"
primaryColor     = "#4f46e5"
backgroundColor  = "#f0f2ff"
secondaryBackgroundColor = "#ffffff"
textColor        = "#1e1b4b"
font             = "sans serif"

[logger]
level            = "warning"     # suppress info noise in production
